# Context-watchdog is a library and does not require environment variables by default.
# These optional values are examples for wrapper applications.

CONTEXT_WATCHDOG_RELEVANCE_MIN=0.35
CONTEXT_WATCHDOG_REDUNDANCY_MAX=0.85
CONTEXT_WATCHDOG_CONFLICT_MIN=0.70
