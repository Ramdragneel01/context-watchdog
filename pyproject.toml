# Generated by GitHub Copilot
[build-system]
requires = ["setuptools>=69.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "context-watchdog"
version = "0.2.0"
description = "Guardrail library to score and filter RAG context chunks before LLM calls."
readme = "README.md"
requires-python = ">=3.11"
license = "MIT"
authors = [{ name = "context-watchdog maintainers" }]
dependencies = [
    "sentence-transformers>=3.0.1",
    "transformers>=4.46.0",
    "langchain>=0.3.7",
    "rich>=13.9.4",
]
keywords = ["rag", "guardrails", "retrieval", "langchain", "llm"]

[project.optional-dependencies]
dev = ["pytest>=8.3.3"]

[tool.setuptools.packages.find]
where = ["."]
include = ["src*", "integrations*"]
